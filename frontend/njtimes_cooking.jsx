import React from 'react';
import ReactDOM from 'react-dom'

document.addEventListener('DOMContentLoaded', () => {
    const root = document.getElementById('root');
    // const store = createStore();
    ReactDOM.render(<h1>IT'S HAPPENING</h1>, root)
})